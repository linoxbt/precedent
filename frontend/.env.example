# Set both to point the UI at a real deployed Precedent Engine contract.
# Leave unset to run entirely against the seeded mock data in src/lib/mockData.ts.
NEXT_PUBLIC_PRECEDENT_ENGINE_ADDRESS=
NEXT_PUBLIC_GENLAYER_RPC_URL=
